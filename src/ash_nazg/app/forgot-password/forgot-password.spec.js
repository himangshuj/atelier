describe( 'reset password section', function() {
    beforeEach( module( 'sokratik.forgot-password' ) );

    it( 'should have a dummy test', inject( function() {
        expect( true ).toBeTruthy();
    }));
});