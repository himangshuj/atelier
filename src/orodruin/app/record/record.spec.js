describe( 'start section', function() {
    beforeEach( module( 'sokratik.orodruin.record' ) );

    it( 'should have a dummy test', inject( function() {
        expect( true ).toBeTruthy();
    }));
});